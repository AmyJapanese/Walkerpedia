---
aliases:
---

```dataviewjs
/***** 設定 *****/
const ZERO_PAD = 3;
const EXCLUDE_PATTERNS = [
  "/(Templates/", "/template/", "/attachments/", "/_assets/",
  "/Daily/", "/Diary/", "/Journal/", "/_archive/"
];
const INCLUDE_TAGS = []; 
const START = 101;     // この索引の開始番号
const END   = 200;   // この索引の終了番号

/***** 収集 *****/
let pages = dv.pages()
  .where(p => p.file.ext === "md")
  .where(p => p.file.path !== dv.current().file.path)
  // 除外パターン
  .where(p => {
    for (let x of EXCLUDE_PATTERNS) {
      if (p.file.path.includes(x)) return false;
    }
    return true;
  })
  // タグ制限
  .where(p => {
    if (INCLUDE_TAGS.length === 0) return true;
    return (p.file.tags ?? []).some(t => INCLUDE_TAGS.includes(t));
  })
  .sort(p => p.file.ctime, 'asc');

/***** 範囲でスライス *****/
let sliced = pages.slice(START - 1, END);  // 1〜100件だけ

/***** 出力 *****/
const header = ["#", "タイトル", "作成日", "更新日", "パス"];
const rows = sliced.map((p, i) => {
  const num = String(START + i).padStart(ZERO_PAD, "0");
  const c = p.file.ctime.toFormat("yyyy-MM-dd");
  const m = p.file.mtime.toFormat("yyyy-MM-dd");
  return [num, p.file.link, c, m, p.file.folder];
});

dv.table(header, rows);
```