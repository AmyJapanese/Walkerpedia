[build]
  base    = "site"          # Hugo のルート
  publish = "site/public"   # 出力先
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.148.2"  # さっき出てたバージョン
  HUGO_ENABLEGITINFO = "true"

[context.production.environment]
  HUGO_ENV = "production"
